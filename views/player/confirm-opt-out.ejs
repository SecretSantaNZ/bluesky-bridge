<form hx-post="/player/opt-out" id="confirm-opt-out" hx-swap="none">
  <div class="space-y-8">
    <h1>Opt out</h1>
    <p>You can opt out at any time, life happens sometimes and we'd rather you told us than ghost us,</p>
    <p>If you opt out after the Giftees are assigned you will not be able to opt back in so make sure you are sure.</p>
    <%- include('../partials/error.ejs', { elementId: 'confirm-opt-out-error' }) %>
    <div class="flex items-center">
      <button @click.prevent="$dispatch('ss-close-modal')" class="text-gray-400">Close</button>
      <div class="flex-grow"></div>
      <%- include('../partials/activity-spinner.ejs') %>
      <input type="submit" name="save" value="Opt out" class="rounded-lg w-lg px-8 py-2 bg-red-600 hover:bg-red-400 text-white drop-shadow-lg focus:ring ring-sky-500 outline-none" />
    </div>
  </div>
  <input type="hidden" name="csrfToken" value="<%= csrfToken %>"/>
<form>