{% from "common/card.njk" import card %}
{% call card() %}
<div class="flex flex-col space-y-4 text-left">
  <h1>Fix matches</h1>
  <div class="flex flex-col items-center">
    {% if criticalMatchIssues > 0 %}
      <div class="text-3xl text-red-600 font-semibold">{{ criticalMatchIssues }}</div>
      <div class="text-sm">Critical issues</div>
    {% elif warnMatchIssues > 0 %}
      <div class="text-3xl text-orange-400 font-semibold">{{ warnMatchIssues }}</div>
      <div class="text-sm">Match issues</div>
    {% else %}
      <div class="text-3xl text-green-600 font-semibold">No issues</div>
    {% endif %}
  </div>
  <p class="text-center text-xl">
    <a href="/admin/fix-matches">Fix matches</a>
  </p>
  <p>Fix any issues with the matches</p>
  <div class="pl-6">
    <ul class="list-disc space-y-2">
      <li>Create last new match chains after the main matching is done</li>
      <li>Fix too many Santas / too many giftees</li>
      <li>Assign Santas to players without</li>
    </ul>
  </div>
</div>
{% endcall %}